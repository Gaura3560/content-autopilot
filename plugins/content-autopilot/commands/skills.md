---
description: "Content Autopilotの全129スキル一覧を表示"
disable-model-invocation: true
---

List all available skills in this plugin by running:
```bash
ls ${CLAUDE_PLUGIN_ROOT}/skills/ | head -20
```
Then show the total count and explain that the user can invoke any skill with `/skill-name`.
